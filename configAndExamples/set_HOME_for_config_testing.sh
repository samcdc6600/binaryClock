#!/bin/tcsh
# THIS SCRIP DOES NOT SEEM TO BE WORKING. WHEN RUN AS
# ". set_HOME_for_config_testing.sh" (that is useing the so called
# "dot space" syntax.) We think this may have something to do with tcsh.
# We use this script to change the value of the HOME shell variable so that we
# can use a test configuration file.
set HOME=\"~/softDev/binaryClock/configAndExamples/\"
